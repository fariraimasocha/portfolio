/** Hero claim — exact copy requested by the user. */
export default function HeroHeadline() {
  return (
    <h1 className='max-w-5xl text-[clamp(3rem,11vw,9rem)] font-medium leading-[0.86] tracking-[-0.045em] [text-wrap:balance]'>
      <span className='block text-muted-foreground'>I turn ideas</span>
      <span className='block'>into products.</span>
    </h1>
  )
}
